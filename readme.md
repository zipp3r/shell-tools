#Usage:
##type in
```mount.sh <local dir> <remote host> <remote user> <remote dir>```
##or with config (local .mount)
```
project=<remote host>
dir=<local dir>
remotedir=<remote dir>
user=<remote user>
```


